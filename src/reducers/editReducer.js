import * as constants from '../constants';

const initialState = {
};

function auth(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default auth;
